name: 🤖 AI News Daily Fetch

on:
  schedule:
    - cron: '0 20 * * *'  # 毎日 05:00 JST (UTC 20:00)
  workflow_dispatch:      # GitHub UI から手動実行も可能

jobs:
  fetch-news:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install feedparser requests

      - name: Fetch AI news
        run: python scripts/fetch_ai_news.py

      - name: Commit and push updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ai-news/data/
          git diff --staged --quiet || (
            git commit -m "🤖 AI news update $(TZ=Asia/Tokyo date +'%Y-%m-%d')" &&
            git push
          )
